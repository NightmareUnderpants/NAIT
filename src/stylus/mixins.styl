card(padding = 12px)
  border 1px solid borderColor
  border-radius 8px
  padding padding

table-block(pad = 12px)
  background #1a1a1a
  border 1px solid rgba(255,255,255,.18)
  border-radius 8px
  padding pad